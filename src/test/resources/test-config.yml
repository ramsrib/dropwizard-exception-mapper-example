database:
  driverClass: org.h2.Driver
  user: sa
  password: sa
  url: jdbc:h2:./target/test
  properties:
      hibernate.hbm2ddl.auto: create

logging:
  level: INFO
  loggers:
    com.dropwizard.example: DEBUG
    org.hibernate.SQL:
      level: DEBUG